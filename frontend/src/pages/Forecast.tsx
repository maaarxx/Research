// Placeholder — next step: pick historical range + forecast horizon,
// "Generate Forecast" button -> POST /api/forecast -> results table + chart.
export default function Forecast() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold text-gray-900">Forecast</h1>
      <p className="text-sm text-gray-500 mt-1">
        Generate ETS-based emission forecasts. Coming soon.
      </p>
    </div>
  )
}
