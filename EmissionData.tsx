// Placeholder — next step is a CSV/Excel upload widget (Year | Electricity |
// Transportation | Waste) that posts to POST /api/emissions/upload,
// shows a preview + validation errors, and a table of saved records.
export default function EmissionData() {
  return (
    <div className="p-8">
      <h1 className="text-xl font-semibold text-gray-900">Emission Data</h1>
      <p className="text-sm text-gray-500 mt-1">
        Upload and manage historical emission records. Coming soon.
      </p>
    </div>
  )
}
